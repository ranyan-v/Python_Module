[project]
name = "matrix-loading"
version = "0.1.0"
description = ""
authors = [
    {name = "rjiao"}
]
requires-python = ">=3.9"
dependencies = [
    "pandas (>=2.1.0)",
    "numpy (>=1.25.0)",
    "matplotlib (>=3.7.2)"
]

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
